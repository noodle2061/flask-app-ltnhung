# Sao chép file này thành .env và điền các giá trị thực tế vào file .env
# KHÔNG commit file .env lên Git! Thêm .env vào file .gitignore của bạn.

# Firebase
# Đường dẫn đầy đủ đến tệp JSON key của Firebase Service Account
FIREBASE_SERVICE_ACCOUNT_KEY_PATH="nhandientienghetapp-firebase-adminsdk-fbsvc-xxxxxxxxxx.json"

# AWS S3 Credentials
# QUAN TRỌNG: Nên sử dụng IAM Roles trên EC2/ECS thay vì Access Keys nếu có thể.
AWS_ACCESS_KEY_ID="YOUR_AWS_ACCESS_KEY_ID"
AWS_SECRET_ACCESS_KEY="YOUR_AWS_SECRET_ACCESS_KEY"
AWS_S3_BUCKET_NAME="your-s3-bucket-name"
AWS_S3_REGION="your-aws-region" # ví dụ: ap-southeast-1

# Cấu hình Server (Tùy chọn, có thể để mặc định trong config.py)
# FLASK_HOST="0.0.0.0"
# FLASK_PORT=5000
# UDP_HOST="0.0.0.0"
# UDP_PORT=5005

# Cấu hình khác (Tùy chọn)
# LOG_LEVEL="INFO"
